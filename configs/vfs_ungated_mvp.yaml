defaults: vfs_weighted_mvp.yaml

project:
  run_tier: resume-mvp-weight-ablation

weighting:
  verifier:
    pass: 1.0
    unknown: 1.0
    fail: 1.0

training_view:
  output_name: vfs_ungated_b50

training:
  input_path: artifacts/resume_mvp/data/training_views/round_0/vfs_ungated_b50.parquet
  output_dir: artifacts/resume_mvp/checkpoints/vfs_ungated_b50_seed42

evaluation:
  output_dir: artifacts/resume_mvp/evaluation/vfs_ungated_b50
