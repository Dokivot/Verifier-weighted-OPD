defaults: base.yaml

project:
  run_tier: recommended-scheme-1

contamination:
  eval_paths:
    - artifacts/data/eval/math500.parquet
    - artifacts/data/eval/aime_2024.parquet
    - artifacts/data/eval/aime_2025.parquet

rollout:
  input_path: artifacts/data/contamination/train_clean.parquet

verification:
  prompt_path: artifacts/data/contamination/train_clean.parquet
