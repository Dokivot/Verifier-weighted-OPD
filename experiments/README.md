# Experiments

Store compact, immutable experiment descriptors here: config path, git commit, input manifest IDs, checkpoint URI, benchmark run IDs and final decision. Large tensors and raw outputs stay in the artifact store.
